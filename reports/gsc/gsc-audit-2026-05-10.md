# GSC Audit — 2026-05-10

Run: `mycalcfinance-gsc-audit-fixes` (weekly, Sunday 7am).
Property: `https://mycalcfinance.com/` (URL-prefix). GSC service-account auth: OK.
Window: 2026-04-10 → 2026-05-08 (28d, accounting for ~2d GSC lag).

## 1. Summary

| Category | Found | Auto-fixed | Flagged for Rizwan |
|---|---:|---:|---:|
| Coverage / indexing | 70 / 79 inspected URLs not "Submitted and indexed" | 0 | 70 (all driven by stale sitemap — see §2 / §7) |
| Core Web Vitals | not measured (PSI not run) | 0 | data gap noted |
| Low-CTR rewrites | 2 candidates (28d, pos 5–20, ≥100 impr) | 2 | 0 |
| Internal 4xx | 1 | 0 | 1 (in untracked draft pair — see §5) |
| Internal 3xx (single 308) | 56 distinct internal hrefs across 60+ posts | 0 | 1 site-wide pattern (trailing-slash) — see §7 |
| External 4xx | 2 | 0 | 2 (flag-only per skill) |
| External 403 / fetch error | 11 | 0 | 0 (likely bot blocks; informational) |
| Working-tree drift | 6 uncommitted post files | 0 | 1 (see §7) |
| Sitemap freshness | 1 (stale 23 days) | 0 | 1 (see §7) |

**Performance overview (28d).** Site is very small in GSC: 24 pages with any impressions, 1,231 total impressions, 4 clicks (overall CTR 0.32%). Top impression page is `/credit-card-payoff-calculator` (631 impr, position 85.2 — far outside the click-earning band). The two low-CTR rewrite candidates are the only blog URLs with both ≥100 impressions and a position in the actionable 5–20 band.

## 2. Coverage fixes

GSC URL-Inspection sample: 79 URLs (all 63 sitemap-listed `/blog/*` posts + 15 calculator endpoints + homepage).

| Coverage state | Count | Notes |
|---|---:|---|
| Submitted and indexed | 9 | Healthy. |
| Crawled - currently not indexed | 3 | `/blog/debt-to-income-ratio-mortgage-guide-2026`, `/blog/home-affordability-guide-2026`, `/blog/break-even-analysis-side-hustles` — Google has crawled but chose not to index. |
| Page with redirect | 1 | `/mortgage-calculator` (sitemap entry 308-redirects to `/mortgage-calculator/`). |
| URL is unknown to Google | 66 | Google has no record of these URLs at all. |

**No autonomous coverage fixes were applied.** The dominant signal — 66/79 URLs "URL is unknown to Google" — is not fixable per-page. It correlates directly with the sitemap being stale (last submitted **2026-04-17**, ~23 days ago, 0 of 177 submitted URLs reported indexed). Re-submitting/refreshing the sitemap is the only effective action and requires either:
- the GSC UI (`Sitemaps → Resubmit`), or
- a service account with **read-write** scope (`https://www.googleapis.com/auth/webmasters`); the current bot has read-only.

The URL-Inspection `requestIndexing` endpoint is browser-only and remains out of scope for unattended runs.

**Action:** flagged in §7. Re-indexing requests (up to 10) for the three "Crawled - currently not indexed" URLs should be made manually by Rizwan in the GSC UI after the sitemap refreshes.

## 3. CWV findings

**Data gap.** PageSpeed Insights API was not invoked this run, and CrUX-via-BigQuery is not configured for this site. Neither the GSC Core Web Vitals nor the Mobile Usability reports are exposed on the public Search Console API, so no CWV numbers are reported in this audit.

Static-export observation worth tracking: every blog post uses an inline `<svg>` hero (no `<img>` tags found in any of the 67 post bodies; image dimension audit returned 0 issues). LCP is therefore likely paint-driven by the hero SVG and the post H1, both server-rendered — should be healthy on a Vercel static deploy.

**Top 3 template-level CWV follow-ups for Rizwan (judgment calls — not auto-applied):**
1. Run a one-off PSI sweep over the 9 currently-indexed URLs to establish a baseline LCP/INP/CLS, then enable a weekly PSI pull from `src/lib/` so this section can produce numbers next week.
2. The blog template at `app/blog/[slug]/page.jsx` should be reviewed for unused/unhydrated client components — the static export includes a Next runtime bundle that's likely oversized for content-only pages.
3. Confirm the static `/out` build is what Vercel is serving (not an on-demand SSR fallback) for `/blog/*` routes.

## 4. Title / meta rewrites

Rewrites applied: **2** (cap of 10).

| Slug | Field | Old → New | Top queries (28d) |
|---|---|---|---|
| `debt-to-income-ratio-mortgage-guide-2026` | `meta_title` | `DTI Ratio for Mortgage 2026: Typical Range & Guidelines` (54) → `Debt-to-Income Ratio for Mortgage 2026: Typical Range` (54) | "debt-to-income ratio mortgage typical range 2026" (23 impr, pos 9.7); "current debt-to-income ratio guidelines for mortgages 2026" (2 impr); "good debt-to-income ratio for mortgage 2026" (2 impr) |
| `debt-to-income-ratio-mortgage-guide-2026` | `meta_description` | `Typical mortgage DTI ratio in 2026: 36% front-end, 43% CFPB QM limit, 50% FHA max. See guidelines by loan type and ways to lower DTI.` (134) → `Debt-to-income ratio for a mortgage in 2026: 36% front-end, 43% CFPB QM limit, 50% FHA max. Guidelines by loan type and ways to lower your DTI.` (147) | (same as above) |
| `how-much-down-payment-house-2026` | `meta_title` | `How Much Down Payment Needed for a House in 2026?` (49) → `How Much Down Payment Needed to Buy a House in 2026?` (52) | "how much down payment needed to buy a house 2026" (11 impr, pos 15.3); "current minimum down payment requirements for mortgages us 2026" (5 impr, pos 9.8) |
| `how-much-down-payment-house-2026` | `meta_description` | `Down payment needed for a house in 2026: conventional 3%, FHA 3.5%, VA/USDA 0%. Yes, you can buy with less than 20% down — see minimums by loan.` (145) → `How much down payment to buy a house in 2026: conventional 3%, FHA 3.5%, VA/USDA 0%. Minimum requirements by loan type — buy with under 20% down.` (146) | (same as above) |

**Rationale.** Both pages already had reasonably-aligned `meta_*` from the 2026-04-25 audit but were ranking at position 12–13 with 0 clicks across 295 combined impressions. The new wording moves the highest-impression query phrasing into the exact title (e.g. "Debt-to-Income Ratio … Typical Range" verbatim from the top query, and "Down Payment Needed to Buy a House" verbatim from the top query) so SERP snippets bold the matched terms — typically the strongest CTR lever available without changing rank.

`updated_at` bumped to `2026-05-10T04:17:00.000Z` on both files. All other frontmatter fields preserved byte-for-byte. `title` and `excerpt` were NOT touched.

**Not rewritten this run** (despite appearing in the candidate set):
- `/blog/hsa-calculator-guide-2026` — only 58 impr (below 100 threshold).
- `/blog/home-affordability-guide-2026` — only 27 impr (below threshold).
- `/credit-card-payoff-calculator` — 631 impr but position 85 (far outside actionable band; rank improvement, not snippet, is what's needed).

## 5. Link fixes

Crawled 514 `<a href>` instances across 67 post bodies (127 unique hrefs: 58 internal, 69 external). All probed.

**Internal 4xx — 1 found, 0 auto-fixed:**

| Post (file) | Bad href | Status | Action |
|---|---|---|---|
| `standard-deduction-vs-itemizing-2026.md` *(untracked)* | `/blog/quarterly-estimated-taxes-self-employed-2026` | 404 | **Skipped.** Both the source post and the link target (`quarterly-estimated-taxes-self-employed-2026.md`) are untracked drafts in the working tree — not part of the GSC audit's scope to push. Will resolve itself once the content-refresh workflow commits the draft pair. Flagged in §7. |

**Internal 3xx — 56 distinct hrefs, 0 auto-fixed:**

Every single internal link omitting a trailing slash hits a 308 redirect to the slashed canonical. Examples (with the count of posts each appears in):

| Bad href (no slash) | Final URL (308) | Posts affected |
|---|---|---:|
| `/retirement-calculator` | `/retirement-calculator/` | 16 |
| `/credit-card-payoff-calculator` | `/credit-card-payoff-calculator/` | 14 |
| `/tax-bracket-calculator` | `/tax-bracket-calculator/` | 14 |
| `/compound-interest-calculator` | `/compound-interest-calculator/` | 18 |
| `/savings-goal-calculator` | `/savings-goal-calculator/` | 22 |
| `/investment-return-calculator` | `/investment-return-calculator/` | 15 |
| `/budget-planner` | `/budget-planner/` | 12 |
| `/mortgage-calculator` | `/mortgage-calculator/` | 18 |
| `/salary-calculator` | `/salary-calculator/` | 12 |
| ...46 more (mix of `/blog/<slug>` and `/<calc>` URLs) | | |

These are single-hop 308s, not multi-hop chains. The SKILL doc rule (§4) specifically scopes the auto-rewrite to "redirect chains (3xx → 3xx → 200)", so single hops are intentionally **not** rewritten in this run. The pattern is high-volume and high-value — see §7 for the recommended approach.

**External 4xx — 2 found, flag-only per skill:**

| Post (file) | Bad href | Status |
|---|---|---|
| `arm-vs-fixed-rate-mortgage-2026.md` | `https://www.bankrate.com/mortgages/arm-loan-rates/` | 405 (HEAD blocked; page may render fine in browsers) |
| `how-to-calculate-savings-rate-formula-2026.md` | `https://newsroom.bankofamerica.com/content/newsroom/press-releases/2025/07/confronted-with-higher-living-costs--72--of-young-adults-take-ac.html` | 404 (genuinely broken) |

**External 403 / abort — informational (likely bot blocks, not real errors):**
- `https://www.ssa.gov/...` (6 paths) — SSA serves 403 to non-browser UAs; pages work for users.
- `https://www.fha.com/fha_requirements_mortgage_insurance` — 403 to bot UA.
- `https://www.bogleheads.org/forum/viewtopic.php?t=370410` — 403 to bot UA.
- `https://bipartisanpolicy.org/explainer/...` — 403 to bot UA.
- `https://my529.org/2025/10/...` — request aborted (timeout).
- `https://www.usnews.com/banking/articles/2026-financial-wellness-survey` — request aborted (timeout).

These do not warrant action; they're noted only so they don't show up as "new" next week.

## 6. Commit

| | |
|---|---|
| Branch | `master` |
| Local SHA | `a17a79f0b09f36ab6d2bd938220d4b8da268831a` |
| Commit message | `seo: gsc audit fixes 2026-05-10` |
| Files staged | `content/blog/debt-to-income-ratio-mortgage-guide-2026.md`, `content/blog/how-much-down-payment-house-2026.md` (explicit add — bulk `git add content/blog/` was deliberately avoided so unrelated working-tree drift would not be bundled in) |
| Parent | `58499e8 blog: add fha-vs-conventional-loan-2026` |
| Pushed to `origin/master` | **NO — push blocked.** |

**Push failure.** `git push origin master` returned:
```
remote: Invalid username or token. Password authentication is not supported for Git operations.
fatal: Authentication failed for 'https://github.com/rshoaib/finance-calculators-hub.git/'
```

The stored GitHub PAT in the run's reference memory (last reset 2026-05-08) was already noted as **expired**. The commit is on local `master` and will fast-forward cleanly once auth is restored. **Per the SKILL doc, no force-push, amend, or history-rewrite was attempted.** The 7am cron-pre-content-refresh window means the 8am `mycalcfinance-content-refresh` task will see this commit on its local checkout but will likewise be unable to push without the new token.

Vercel deploy spot-check was therefore skipped (nothing to verify on prod).

## 7. Manual follow-ups

In rough priority order:

1. **Rotate the GitHub PAT.** Mint a new fine-grained or classic PAT with `Contents: Read and write` on `rshoaib/finance-calculators-hub`, then update the `github_pat_mycalcfinance` memory entry. Without this, every weekly run accumulates orphaned local commits that never reach Vercel. Once rotated, run `git push origin master` from the repo to ship the `a17a79f` commit.
2. **Refresh and re-submit the sitemap.** `https://mycalcfinance.com/sitemap.xml` was last submitted on 2026-04-17 and shows 0/177 indexed. Every new post since then (RMD, FHA-vs-conventional, ETF-vs-mutual-fund, etc.) is invisible to Google's discovery path. Required steps:
   - Regenerate `public/sitemap.xml` (or whichever build step produces it) so `lastmod` reflects current dates and includes the most recent posts. The current sitemap shows every entry stamped `2026-04-17T00:00:00.000Z` — the generator hasn't run since.
   - Use the canonical (trailing-slash) URLs in the sitemap. The current sitemap has `https://mycalcfinance.com/mortgage-calculator` (no slash) which 308-redirects — this is the "Page with redirect" coverage state in §2.
   - `Sitemaps → Resubmit` in the GSC UI for `https://mycalcfinance.com/sitemap.xml`.
3. **Trailing-slash sweep across post bodies.** 56 unique internal hrefs across 60+ posts hit 308 redirects. This isn't a "redirect chain" so the SKILL didn't allow auto-fixing it, but it's the single highest-volume on-site SEO issue right now. Recommend a one-off batch script that:
   - Reads every `content/blog/*.md`,
   - In the HTML body only (post-frontmatter), regex-replaces `href="(/[^"]+[^/])"` → `href="$1/"` for paths matching `/<calculator>` and `/blog/<slug>`,
   - Bumps `updated_at` on changed files,
   - Commits as a single `seo: trailing-slash internal links` change.
   I'd run it as a one-time companion task rather than fold it into the weekly auto-audit.
4. **Working-tree drift.** Before pushing, Rizwan should triage the working-tree state visible during this run:
   - Modified (uncommitted): `biweekly-mortgage-payments-vs-monthly-2026.md`, `required-minimum-distributions-rmd-2026-guide.md`, `roth-401k-vs-traditional-401k-2026.md`.
   - Untracked: `etf-vs-mutual-fund-2026.md`, `quarterly-estimated-taxes-self-employed-2026.md`, `standard-deduction-vs-itemizing-2026.md` (plus matching SVG hero assets in `public/blog-images/`).
   These look like artifacts of an interrupted `mycalcfinance-content-refresh` run that couldn't push. They are also why the §5 internal 404 isn't being auto-fixed — the broken link is between two untracked files. Decide: commit them (and rerun this audit), or `git restore`/`git clean` them.
5. **Stale Git lock files.** `.git/index.lock` and `.git/HEAD.lock` stay around as zero-byte files after every interrupted run (saw 11 such relics this morning, dating back to Apr 25). They aren't blocking subsequent runs (the audit moved them out of the way) but they signal the sandbox can't `unlink` files it owns. Worth a manual `find .git -maxdepth 2 -name '*.lock*' -size 0 -delete` from a non-sandboxed shell.
6. **Re-index the 3 "Crawled - currently not indexed" URLs.** Once the sitemap refresh lands and a few days have passed, request indexing in the GSC UI for: `/blog/debt-to-income-ratio-mortgage-guide-2026`, `/blog/home-affordability-guide-2026`, `/blog/break-even-analysis-side-hustles`.
7. **Replace the genuine external 404** in `how-to-calculate-savings-rate-formula-2026.md` (Bank of America 2025 press release moved). Find a current source for the "young adults / cost of living" stat and update the link or remove it.
