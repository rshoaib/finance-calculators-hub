# MyCalcFinance — Weekly SEO Pulse

**Run date:** 2026-04-15
**Status:** 🟡 issues found (one borderline Core Web Vital on the homepage; no regressions vs. last pulse)

One-liner per area:
- Crawlability & indexing — ✅ clean
- Broken links / HTTP errors — ✅ clean (28/28 spot-checked URLs returned 200)
- Core Web Vitals / page speed — 🟡 homepage LCP borderline (3.98s mobile, lab); calculator + blog templates healthy
- Recent content indexing — ✅ clean (both 2026-04-14 posts indexable with full metadata)

---

## Findings

### Core Web Vitals / page speed — 🟡 homepage LCP borderline
PSI ran successfully this week (key loaded from `.secrets/pagespeed-api-key.txt`, HTTP 200 on all three calls).

| Page (mobile) | Perf score | LCP | CLS | INP |
|---|---|---|---|---|
| `/` | 81 | **3.98 s** | 0 | – (no field data) |
| `/mortgage-calculator` | 98 | 1.3 s | 0 | – |
| `/blog/how-to-calculate-closing-costs-2026` | 100 | 1.4 s | 0 | – |

The homepage LCP (3.98 s) is technically "Needs Improvement" (Poor is strictly >4.0 s), but it sits right on the boundary and is the only metric outside the green zone across the three sampled pages. Supporting numbers on the homepage: FCP 2.5 s, Speed Index 4.5 s, TTI 4.0 s, TBT 70 ms — so the issue is rendering/resource timing on first paint, not JS blocking.

CrUX field data (including real INP) isn't available yet for any of the three URLs — likely still below the traffic threshold. Lab INP is omitted by design (no user interactions in the run).

**Suggested fix:** investigate homepage LCP element — likely the hero image (`hero-credit-score-2026.png` lives in the workspace) or the font preload. Quick wins to try: add `fetchpriority="high"` to the hero image, confirm it's served via Next.js `<Image>` with width/height + priority, and check whether the WOFF2 font preload is blocking text paint. If LCP drifts above 4.0 s next week this moves from yellow to red.

---

## Detail (for reference — clean areas)

**Crawlability.** `robots.txt` returns 200 with `User-agent: * / Allow: /` and correctly declares `Sitemap: https://mycalcfinance.com/sitemap.xml`. Sitemap is valid XML, 175 URLs (up from 161 last pulse — consistent with two new blog posts + 12 new programmatic "how-long-to-save-…" pages), proper lastmod timestamps. Homepage returns 200 with populated title, meta description, and canonical; no `noindex`/`nofollow` robots meta.

**Link spot-check.** 28 URLs tested, all 200 on a followed redirect: homepage, `/blog`, `/about`, `/contact`, `/privacy-policy`, 18 calculator pages pulled from the homepage nav, and the 5 most recent blog posts. (Note: last pulse flagged four 404s that turned out to be my own slug guesses — this run pulled calculator slugs directly from the homepage nav to avoid the same trap.)

**Recent content.** The two newest posts (both published 2026-04-14) are still the freshest content and remain healthy:
- `/blog/how-to-calculate-closing-costs-2026` — 200, title + meta description present, indexable (default robots).
- `/blog/dollar-cost-averaging-explained-examples-2026` — 200, title + meta description present, indexable.

No new posts were published in the past 24 hours, so the "most recent" set hasn't rotated since the baseline pulse.

---

## Comparison to previous pulse (2026-04-15 baseline, earlier run)

| Area | Last pulse | This pulse | Delta |
|---|---|---|---|
| Crawlability | ✅ clean (161 URLs) | ✅ clean (175 URLs) | +14 programmatic URLs, expected |
| Broken links | ✅ 27/27 | ✅ 28/28 | Same — no regressions |
| Core Web Vitals | ⚠️ deferred (PSI 429) | 🟡 data captured, homepage LCP borderline | **NEW signal** — PSI key worked, and first real measurement shows homepage LCP at the edge |
| Recent content | ✅ same 2 posts from 2026-04-14 | ✅ same 2 posts from 2026-04-14 | No new posts published this week |

**New this week:** homepage LCP ≈ 3.98 s is the first concrete Core Web Vitals signal we have — last pulse couldn't measure it. It's the one yellow item; calculator and blog templates are both green.

---

## Notes from the run
- PSI API key loaded from `<key loaded from .secrets/pagespeed-api-key.txt>`; all three PSI calls returned HTTP 200. No 429/403 this week.
- Task spec resolved `<workspace>` to the current session's `/sessions/nice-trusting-volta/mnt/mycalcfinance/` at runtime (not hardcoded).
- No new blog posts published since the baseline pulse — the two most-recent posts are unchanged. Two posts per week cadence means another post is expected later this week.
- Audit-only: no changes were made to the site.
