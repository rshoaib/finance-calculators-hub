<?xml version="1.0" encoding="UTF-8"?>
<!--
  This static sitemap is SHADOWED by app/sitemap.js in Next.js — the dynamic
  route wins at /sitemap.xml. This file is kept here only so git history
  remains clean; the sandbox's rm couldn't unlink it due to mount perms. Safe
  to delete locally with `git rm public/sitemap.xml`.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" />
